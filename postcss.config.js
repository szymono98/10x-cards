/**
 * This type annotation ensures TypeScript can provide type checking and autocompletion
 * for the PostCSS configuration object, as defined by the `postcss-load-config` package.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
